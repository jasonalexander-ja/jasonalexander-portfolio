import React from 'react';

import {
    Typography
} from '@material-ui/core';

export const CV = () => {

    return(
        <>
            <Typography variant="h1">Hello World</Typography>
        </>
    );
}

export default CV;
